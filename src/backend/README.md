# iHRM Backend

iHRM (IntrustDSS Human Resource Management) — Laravel 12 backend API.

## Setup

```bash
cp .env.example .env
composer install
php artisan key:generate
php artisan migrate
php artisan serve
```

## Testing

```bash
composer test
```
